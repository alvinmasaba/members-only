<div id="<%= dom_id post %>">
  <p>
    <strong><%= post.title %></strong>
  </p>

  <p>
    <%= post.body %> - <em><strong><%= User.find(post.user_id).username %></strong> on <%= post.created_at.to_fs(:long) %> </em>
  </p>
</div>
